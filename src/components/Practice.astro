---
interface Props {
  items: { title: string; description: string }[];
}

const { items } = Astro.props;
---

<ul class="practice">
  {
    items.map((item) => (
      <li>
        <h3>{item.title}</h3>
        <p>{item.description}</p>
      </li>
    ))
  }
</ul>
