---
interface Props {
  label: string;
  id: string;
}

const { label, id } = Astro.props;
---

<h2
  id={id}
  class="mb-6 border-t border-border pt-8 font-mono text-[var(--text-step-minus-1)] font-normal tracking-[0.08em] text-muted uppercase"
>
  {label}
</h2>
